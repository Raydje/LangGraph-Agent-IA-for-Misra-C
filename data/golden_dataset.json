[  
  {
    "is_compliant": false,
    "query": "Does this memory allocation code comply with MISRA-C? Check for dynamic memory usage violations.",
    "code_snippet": "ImuFrame_t *adcs_alloc_frame_buffer(int count)\n{\n    ImuFrame_t *buf = (ImuFrame_t *)malloc((size_t)count * sizeof(ImuFrame_t));\n    return buf;\n}\n\nvoid adcs_free_frame_buffer(ImuFrame_t *buf)\n{\n    free(buf);\n    buf = NULL;\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": false,
    "query": "Validate this function for MISRA-C compliance. Is there unreachable code or undefined behavior with bit-shift operations?",
    "code_snippet": "Vec3_t estimate_gyro_bias(void)\n{\n    Vec3_t   bias;\n    float    sum_x = 0.0f;\n    float    sum_y = 0.0f;\n    float    sum_z = 0.0f;\n    uint8_t  i;\n    int      count;\n    int      shift_amount = 40;\n\n    count = MAX_ATTITUDE_HISTORY;\n\n    for (i = 0U; i < (uint8_t)MAX_ATTITUDE_HISTORY; i++)\n    {\n        sum_x += g_imu_history[i].gyro_rads.x;\n        sum_y += g_imu_history[i].gyro_rads.y;\n        sum_z += g_imu_history[i].gyro_rads.z;\n    }\n\n    bias.x = sum_x / (float)count;\n    bias.y = sum_y / (float)count;\n    bias.z = sum_z / (float)count;\n\n    count = count << shift_amount;\n    (void)count;\n\n    return bias;\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": false,
    "query": "Check this function for MISRA-C compliance. Focus on unreachable code after return statements.",
    "code_snippet": "int evaluate_safe_mode(Vec3_t omega, float battery_v)\n{\n    float omega_mag;\n    int   enter_safe;\n    int   x = 10;\n\n    omega_mag = sqrtf(omega.x*omega.x + omega.y*omega.y + omega.z*omega.z);\n\n    if (x > 5)\n    {\n        g_adcs_cycle_count++;\n    }\n    else\n    {\n        g_adcs_cycle_count--;\n    }\n\n    enter_safe = 0;\n\n    if (omega_mag > SAFE_MODE_THRESHOLD)\n    {\n        enter_safe = 1;\n        return enter_safe;\n    }\n    else if (battery_v < 6.5f)\n    {\n        enter_safe = 1;\n        return enter_safe;\n    }\n    else\n    {\n        return enter_safe;\n    }\n\n    g_adcs_cycle_count++;\n    return enter_safe;\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": false,
    "query": "Is this function MISRA-C compliant? Look for buffer overflow risks and unsafe string operations.",
    "code_snippet": "void write_mission_log(char *label, float value)\n{\n    char  msg_buf[16];\n    char  full_label[] = \"ADCS_LOG: this label is longer than the buffer\";\n\n    strcpy(msg_buf, full_label);\n\n    printf(\"%s = %.4f\\n\", label, (double)value);\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": false,
    "query": "I'm working on this function and need to know if it's MISRA compliant for if a function returns error information, then that error information shall be tested.",
    "code_snippet": "void D_4_7 ( void )\n{\n    uint8_t error_status;\n    uint16_t fun_ret = get_uint16 ( );\n    uint16_t param   = get_uint16 ( );\n\n    ( void ) add ( &fun_ret, &param );\n\n    error_status = add ( &fun_ret, &param ); /* Also breaks R.2.2 */\n\n    /* Compliant 'error' information is assigned to error_status, and subsequently tested. */\n    error_status = add ( &fun_ret, &param );\n    if ( error_status != 0U )\n    {\n       fun_ret = 0U;\n    }\n\n    use_uint16 ( fun_ret );\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": true,
    "query": "Does this memory allocation code comply with MISRA-C? Check for dynamic memory usage violations.",
    "code_snippet": "typedef struct { int data[10]; } ImuFrame_t; /* Placeholder for actual ImuFrame_t definition */\n\n#define MAX_IMU_FRAMES 100\nstatic ImuFrame_t adcs_static_frame_buffer[MAX_IMU_FRAMES];\nstatic bool adcs_frame_buffer_in_use = false;\n\nImuFrame_t *adcs_alloc_frame_buffer(int count)\n{\n    ImuFrame_t *buf = NULL;\n\n       if ((count > 0) && (count <= (int)MAX_IMU_FRAMES) && (!adcs_frame_buffer_in_use))\n    {\n        buf = adcs_static_frame_buffer;\n        adcs_frame_buffer_in_use = true;\n    }\n    return buf;\n}\n\nvoid adcs_free_frame_buffer(ImuFrame_t *buf)\n{\n     if (buf == adcs_static_frame_buffer)\n    {\n        adcs_frame_buffer_in_use = false;\n    }\n    /* Setting 'buf = NULL' here would only affect the local copy of the pointer, not the caller's.}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": true,
    "query": "Is this function MISRA-C compliant? Look for buffer overflow risks and unsafe string operations.",
    "code_snippet": "#include <string.h>#include <stdio.h>void write_mission_log(char *label, float value){printf(\"%s = %.4f\\n\", label, (double)value);}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": true,
    "query": "Does this function violate MISRA-C rules? Check for octal literals and numeric constant issues.",
    "code_snippet": "Vec3_t bdot_control(Vec3_t mag_prev, Vec3_t mag_curr, float dt)\n{\n    Vec3_t  bdot;\n    Vec3_t  dipole;\n    float   norm;\n    float   gain = 493.0f;\n\n    bdot.x = (mag_curr.x - mag_prev.x) / dt;\n    bdot.y = (mag_curr.y - mag_prev.y) / dt;\n    bdot.z = (mag_curr.z - mag_prev.z) / dt;\n\n    norm = sqrtf(bdot.x*bdot.x + bdot.y*bdot.y + bdot.z*bdot.z);\n\n    if (norm < 1e-9f)\n    {\n        dipole.x = 0.0f;\n        dipole.y = 0.0f;\n        dipole.z = 0.0f;\n        return dipole;\n    }\n\n    dipole.x = -gain * bdot.x / norm;\n    dipole.y = -gain * bdot.y / norm;\n    dipole.z = -gain * bdot.z / norm;\n\n    return dipole;\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": true,
    "query": "Check this function for MISRA-C compliance. Focus on unreachable code after return statements.",
    "code_snippet": "int evaluate_safe_mode(Vec3_t omega, float battery_v)\n{\n    float omega_mag;\n    int   enter_safe;\n    int   x = 10;\n    int   ret_val;\n\n    omega_mag = sqrtf(omega.x*omega.x + omega.y*omega.y + omega.z*omega.z);\n\n    if (x > 5)\n    {\n        g_adcs_cycle_count++;\n    }\n\n    enter_safe = 0;\n\n    if (omega_mag > SAFE_MODE_THRESHOLD)\n    {\n        enter_safe = 1;\n    }\n    else if (battery_v < 6.5f)\n    {\n        enter_safe = 1;\n    }\n\n    ret_val = enter_safe;\n    return ret_val;\n}",
    "standard": "MISRA C:2023"
  },
  {
    "is_compliant": true,
    "query": "Is this allowed in MISRA-C? It's about if a function returns error information, then that error information shall be tested.",
    "code_snippet": "#include <stdint.h>\n uint16_t get_uint16(void);\nextern uint8_t add(uint16_t *a, uint16_t *b);\nextern void use_uint16(uint16_t val);\n\nvoid D_4_7 ( void )\n{\n    uint8_t error_status;\n    uint16_t fun_ret = get_uint16 ( );\n    uint16_t param   = get_uint16 ( );\n\n    \n    error_status = add ( &fun_ret, &param );\n    if ( error_status != 0U )\n    {\n       fun_ret = 0U;\n    }\n\n   \n    error_status = add ( &fun_ret, &param );\n    if ( error_status != 0U )\n    {\n       fun_ret = 0U;\n    }\n\n    error_status = add ( &fun_ret, &param );\n    if ( error_status != 0U )\n    {\n       fun_ret = 0U;\n    }\n\n    use_uint16 ( fun_ret );\n}",
    "standard": "MISRA C:2023"
  }      
]           